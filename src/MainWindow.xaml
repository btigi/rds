<Window x:Class="rds.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RDS" Height="450" Width="800"
        Icon="icon.ico"
        ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" 
                     Grid.Column="0" 
                     Height="30" 
                     VerticalContentAlignment="Center"
                     KeyDown="SearchTextBox_KeyDown"
                     Margin="0,0,10,0"/>

            <Button x:Name="SearchButton" 
                    Grid.Column="1" 
                    Content="Search" 
                    Width="80" 
                    Height="30"
                    Click="SearchButton_Click"/>
        </Grid>

        <ListView x:Name="ResultsListView" 
                  Grid.Row="1"
                  KeyDown="ResultsListView_KeyDown"
                  MouseDoubleClick="ResultsListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Path" Width="600" DisplayMemberBinding="{Binding DisplayPath}"/>
                    <GridViewColumn Header="File Name" Width="150" DisplayMemberBinding="{Binding FileName}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

